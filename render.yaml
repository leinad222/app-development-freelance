services:
  - type: web
    name: aster-storefront
    runtime: node
    plan: starter
    branch: main
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /
    disk:
      name: aster-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
